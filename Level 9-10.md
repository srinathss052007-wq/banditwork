Level 9-10:

The password for the next level is stored in the file data.txt and is the only line of text that occurs only once. sort data.txt: The sort command is used to sort the lines in a text file.

<img width="1920" height="790" alt="level 9-level 10" src="https://github.com/user-attachments/assets/471d7dcd-73fc-45e8-b71e-5cd440badb9e" />

The pipe symbol | is used to redirect the standard output (stdout) of the command on its left to the standard input (stdin) of the command on its right. In this case, it pipes the sorted output from sort data.txt to the next command, uniq -c.
